package linkedListTrolling

type Node struct {
	value int
	next  *Node
}

// ToDo: Have a set of values be transformed into a linked list, and then sort the linked list.
func main() {
	valueArray := [10]int{1, 8, 3, 5, 1, 6, 2, 0, 5, 1}
}
